{
  "rules": {
    "posts": {
      ".read": true,
      ".indexOn": ["lang", "category", "updatedAt", "createdAt"],
      "$postId": {
        ".write": "auth != null && (root.child('admins').child(auth.uid).val() === true || (!data.exists() && root.child('employees').child(auth.uid).val() === true))",
        ".validate": "newData.isNull() || (newData.hasChildren(['title', 'lang', 'category', 'content', 'date']) && newData.child('title').isString() && newData.child('content').isString() && newData.child('lang').isString() && newData.child('category').isString() && newData.child('date').isString())"
      }
    },

    "admins": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": false
      }
    },

    "employees": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": false
      }
    },

    ".read": false,
    ".write": false
  }
}
